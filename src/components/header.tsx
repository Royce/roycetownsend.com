/** @jsx jsx */
import { jsx } from 'theme-ui';

const Header: React.FC = () => (
  <header
    sx={{
      width: `100%`,
      display: `flex`,
      alignItems: `center`,
    }}
  >
    Royce Townsend
  </header>
);

export default Header;
